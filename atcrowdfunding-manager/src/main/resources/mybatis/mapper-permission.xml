<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.atcrowdfunding.dao.PermissionDao">
	
	<!-- List<Permission> queryAllPermissionidsByUser(User dbUser); -->
	<select id="queryAllPermissionidsByUser" resultType="com.atguigu.atcrowdfunding.bean.Permission">
		select * from t_permission
		where id in (
			select permissionid from t_role_permission
			where roleid in (
				select roleid from t_user_role
				where userid=#{id}
			)
		)
	</select>
	
	<!-- void insertPermission(Permission permission); -->
	<insert id="insertPermission">
		insert into t_permission(name,url,pid) 
		values (#{name},#{url},#{pid})
	</insert>
	
	<!-- void updatePermission(Permission permission); -->
	<update id="updatePermission">
	 	update t_permission set 
	 	name=#{name},url=#{url}
	 	where id = #{id}
	</update>
</mapper>